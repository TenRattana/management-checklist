import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ManagerGroupPermission_dialog = React.memo(() => {
    return (
        <View>
            <Text>ManagerGroupPermission_dialog</Text>
        </View>
    )
})

export default ManagerGroupPermission_dialog

const styles = StyleSheet.create({})